import { Text, View } from "react-native";

export default function SearchNavigation({ navigator }) {
    return (
        <View>
            <Text>
                asd
            </Text>
        </View>
    );
}